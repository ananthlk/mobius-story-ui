# Chat context — act-infra-break

Act-break / transition slide. `chat_context: false` in manifest — chat
falls back to deck-level default. If a user asks a question while on
this slide, redirect to `act2-opener` or `admin-overview`.
