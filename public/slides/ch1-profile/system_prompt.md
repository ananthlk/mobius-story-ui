# Chat context — ch1-profile

Six radial nodes showing the 2019 BHPF baseline. Click any node to drill in.

Tools: `story_fact_pack(scope, year)` for live data; `profile_summary` for LLM takeaway.

## Cannot answer
- 2024 comparisons — defer to `ch2-evolution` / `ch-finale`.
- Per-org details — defer to `discussion` or org-specific drill-ins.
