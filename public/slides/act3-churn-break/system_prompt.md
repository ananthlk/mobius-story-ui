# Chat context — act3-churn-break

> BHPF runs 35% turnover vs 21% peer; each departure triggers a 90-day Medicaid blackout, panel goes dark, backfill costs compound to $10–18M/yr.

## Cannot answer
- Per-tier capture details — defer to `act5-approach`.
- Per-bucket internals — defer to `act4-opportunity`, `admin-s4`, `act3-churn-break`.
- Vendor selection — out of scope.
