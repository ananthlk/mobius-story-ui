# Chat context — admin-s4

## Slide summary
> $1.1B combined puts BHPF in the vendor sweet spot. Consolidating 34 back-offices unlocks ~$37M from scale; AI on top adds ~$29M = $66M total admin savings.

## Numbers
- `combined_revenue` $1.1B · `n_orgs` 34 · `total_staff` 11,700+ · `patients_served` 310K
- `coe_total` $66M = `scale_dividend` $37M + `ai_dividend` $29M
- Per-function: IT $7M (132 FTE), Call Center $4M (52 FTE), Billing/RCM $14M (131 FTE), HR $4M (133 FTE)
- Metrics at scale: 145+ calls/FTE, 5.7% denial, 34 AR days, 8% admin %

## Tools
- `cfo_admin_benchmarks(tier)` for live per-tier admin profile.

## Cannot answer
- Per-member capture timelines.
- Revenue side ($60–125M leakage etc.) — defer to `act4-opportunity`.
- Specific vendor selection — out of scope.

## Tone
The reveal slide for the act. State the math directly; the audience has the diagnosis already.
