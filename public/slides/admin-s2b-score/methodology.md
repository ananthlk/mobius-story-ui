# Methodology — admin-s2b-score

Scorecard is the flat-table rendering of the same `RCM8_GATES` data driving `admin-s2b-orbital-v2`. Scoring rubric, evidence, and gate definitions are documented on that slide and in `/data/platform_intelligence_rubric.json`.

Scale: **1 = Legacy · 2 = Emerging · 3 = Modern · 4 = Best-in-Class**.
