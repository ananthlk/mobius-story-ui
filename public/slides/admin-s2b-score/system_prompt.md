# Chat context — admin-s2b-score

Flat-table view of the 8-gate × 4-vendor scorecard. Same data as `admin-s2b-orbital-v2`. Defer rubric / evidence questions to that slide.

Tools: `get_briefing_context(admin-s2b-orbital-v2)` for the underlying rubric.

Cannot answer: vendor endorsement; specific deployment outcomes.
