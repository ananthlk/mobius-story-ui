# Chat context — admin-s1c

## Slide summary
> Vendor 2:1 ROI math is validated at $500M; BHPF averages $32M; the same $150K fixed cost consumes 50–68% of deal value at small scale.

## Numbers
- `vendor_promise` 2.0x — vendor's pitch.
- `validated_at_m` $500M — where 2:1 actually holds.
- `bhpf_avg_m` $32M — BHPF average org.
- `fixed_cost_k` $150K — sales + integration.
- `fixed_pct_low` 50% / `fixed_pct_high` 68% — fixed-cost share at small tiers.

## What chat can answer
- ROI multiple at any of the 10 tiers (clicking a dot shows the same data).
- Why BHPF's $32M average sits below break-even on standard vendor pricing.

## Cannot answer
- Specific vendor pricing — generic across the EHR/RCM market.
- Forecast ROI under negotiated discounts — not in scope.
